import { useQuery } from '@tanstack/react-query'; import { Link } from 'react-router-dom'; import { api } from '../../shared/services/api';
export default function AssessmentsPage(){const q=useQuery({queryKey:['assessment-queue'],queryFn:async()=>(await api.get('/clinical/assessment-queue')).data});return <div className="space-y-5"><header><p className="text-sm font-semibold uppercase tracking-wider text-brand-700">Clinical intake</p><h1 className="text-3xl font-bold">Assessments</h1><p className="text-slate-500">Open visits and their current assessment status.</p></header><section className="card overflow-x-auto"><table className="w-full min-w-[700px] text-sm"><thead className="border-b text-left text-slate-500"><tr><th className="p-3">Patient</th><th className="p-3">Visit</th><th className="p-3">Care type</th><th className="p-3">Reason</th><th className="p-3">Assessment</th></tr></thead><tbody>{q.isLoading&&<tr><td colSpan={5} className="p-8 text-center">Loading visits…</td></tr>}{q.data?.map((v:any)=><tr className="border-b" key={v.id}><td className="p-3"><Link className="font-semibold text-brand-700" to={`/patients/${v.patient.id}`}>{v.patient.firstName} {v.patient.lastName}</Link><p className="text-xs">{v.patient.patientNumber}</p></td><td className="p-3">{v.visitNumber}</td><td className="p-3">{v.careType}</td><td className="p-3">{v.reason}</td><td className="p-3"><span className={`rounded-full px-2 py-1 text-xs ${v.assessmentStatus==='RECORDED'?'bg-emerald-50 text-emerald-700':'bg-amber-50 text-amber-700'}`}>{v.assessmentStatus}</span></td></tr>)}</tbody></table></section></div>}
